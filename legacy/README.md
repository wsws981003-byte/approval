# Legacy Files (기존 HTML/CSS/JS 버전)

이 폴더에는 React로 변환하기 전의 기존 HTML/CSS/JS 파일들이 보관되어 있습니다.

## 파일 구조

- `index.html` - 기존 메인 HTML 파일
- `js/` - 기존 JavaScript 파일들
- `styles.css` - 기존 CSS 스타일

## 참고사항

- 이 파일들은 참고용으로만 보관됩니다
- 현재 배포는 React 버전만 사용됩니다
- 필요시 참고용으로 활용하세요

